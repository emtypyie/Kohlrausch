Alright so  
i need the macropad to have one 12c 0.91 in oled display which will show the layers and all  
with that there would be 3x3 keyboard matrix to register the keys in that we can set macros and there would be an extra key which would switch the presets there would be 4 layers and each layer would have its own preset like layer1 switch 11 would redirect to youtube but layer 2 switch 11 would redirect to nhentai.net so like that there would be different preset layers but as for start it would be 4  
and we would make an terminal based interface ui for the customisation
u can code it yourself or can use the existing flashed code and change switch in that via emtypyie.cli or just flash the cpp code whatever u want   
but personally i would like to do the change macros from the terminal based ui of the kohlrausch which would run inside emtypyie.cli instead of cpp code cuz for cpp we need to flash each time we make changes and it hurts a lot lol.
